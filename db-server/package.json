{
  "name": "opens-db-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tiny SQL-over-HTTP API exposing dev.db to the OPENS Netlify site.",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "fastify": "^5.0.0"
  }
}
